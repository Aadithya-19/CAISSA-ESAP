# The RTL is finished here and the TESTBENCH is the exercise, so `make
# solution` swaps which python module cocotb runs, not which .sv it builds.
TOPLEVEL    := edge_detect
MODULE      := test_edge_detect
LESSON_KIND := tb
include ../common.mk
