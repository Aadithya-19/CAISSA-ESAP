# This lesson inverts the usual arrangement. The RTL is finished and the
# TESTBENCH is the exercise, so `make solution` swaps which python module
# cocotb runs instead of which .sv it builds.
TOPLEVEL := edge_detect
LESSON_KIND := tb
MODULE_YOURS := test_edge_detect
MODULE_REF   := test_edge_detect_ref
include ../common.mk
